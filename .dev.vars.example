# Ticket 01 requires no local secrets. When a feature introduces one, copy this
# file to .dev.vars and add the value there. Never commit .dev.vars.
#
# EXAMPLE_SECRET=replace-with-a-local-only-value
