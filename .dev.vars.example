# Copy this file to .dev.vars and replace both values with independent random
# secrets of at least 32 characters. Never commit .dev.vars or reuse production
# values locally.

BETTER_AUTH_SECRET=replace-with-an-independent-random-secret
SETUP_SECRET=replace-with-an-independent-random-secret
